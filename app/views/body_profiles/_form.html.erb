<%= form_for(@body_profile) do |f| %>
  <% if @body_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@body_profile.errors.count, "error") %> prohibited this body_profile from being saved:</h2>

      <ul>
      <% @body_profile.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
